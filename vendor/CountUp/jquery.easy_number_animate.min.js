"use strict";
!function (e) {
	e.fn.easy_number_animate = function (t) {
		var n = e.extend({
				start_value: 0,
				end_value: 100,
				duration: 1e3,
				delimiter: ".",
				round: !0,
				before: null,
				after: null
			}, t), r = {DOWN: 0, UP: 1}, a = 1e3, u = e(this), l = Math.ceil(n.duration / (1e3 / 60)), i = n.start_value,
			o = (n.end_value - n.start_value) / l, f = n.start_value < n.end_value ? r.UP : r.DOWN;
		"function" == typeof n.before && n.before(u), function e() {
			if (i !== n.end_value) {
				var t = i + o;
				i = f === r.UP ? t > n.end_value ? n.end_value : t : t < n.end_value ? n.end_value : t, n.round && (t = Math.round(i)), n.delimiter && t >= a && (t = function (e) {
					var t = e.toString();
					if (t.length > 3) {
						for (var r = t.length % 3, a = r || 3, u = t.slice(0, a); a < t.length; a += 3) u += n.delimiter + t.slice(a, a + 3);
						return u
					}
					return e
				}(t)), u.text(t), requestAnimationFrame(e)
			} else "function" == typeof n.after && n.after(u, i)
		}()
	}
}(jQuery);
